# GXLib はじめにお読みください

ようこそ GXLib へ！C++ 初学者でもダブルクリックだけで始められます。

---

## 🚀 最速の始め方（3ステップ）

### ステップ1: サンプルを開く

**`open_examples.bat`** をダブルクリック

- Visual Studio が開き、16 個のサンプルが `Examples/` フォルダに並んでいます
- 初回は依存ライブラリを取得するため数分かかります（2回目以降は数秒）
- CMake や Visual Studio が入っていない場合はインストール方法を案内します

→ **`gxlib_example_01`（hello-sprite）** を右クリック →「スタートアップ プロジェクトに設定」→ **F5** で動きます。

### ステップ2: サンプルを読む・改造する

サンプルは 1 ファイル 50-100 行の小さいコードで、各 1 トピックを学べます：

| # | 内容 | 難易度 |
|---|---|---|
| 01 | 画像表示 + マウス追従 | ★ |
| 02 | 音の再生（SE + BGM）| ★ |
| 03 | キーボード・マウス・ゲームパッド | ★ |
| 04 | 3Dモデル表示 | ★★ |
| 05-06 | ポストエフェクト・カスタムシェーダ | ★★★ |
| 07 | ECS（エンティティ管理）| ★★ |
| 08 | 2D 物理 | ★★ |
| 09 | スケルタルアニメーション | ★★ |
| 10 | カスタムアセット型 | ★★★ |
| 11 | カスタム DSP エフェクト | ★★★ |
| 12 | GUI ウィジェットツリー | ★★ |
| 13 | カスタムウィジェット派生 | ★★★ |
| 14 | ネットワーク通信 (チャット) | ★★ |
| 15 | 3D 物理 (ボックス落下) | ★★★ |
| 16 | IK (視線追従) | ★★★ |

### ステップ3: 自分のゲームを作る

**`create_new_project.bat`** をダブルクリック

- プロジェクト名を入力（例: `MyGame`）
- 自動でプロジェクトフォルダ + Visual Studio ソリューションが作られます
- `MyGame/main.cpp` を好きに書き換えてゲームを作れます

### ステップ4 (必要時): ビルドをクリーンして作り直す

**`rebuild_gxlib.bat`** をダブルクリック

- GXLib のビルドファイル (`build\` フォルダ) だけをクリーン削除して再生成
- **あなたが作った `MyGame\` などの作業フォルダには一切触れません**
- git pull 後 / 設定変更後 / ビルドがおかしくなった時に使う

確認プロンプトあり。間違って押しても即キャンセル可能。

---

## ❓ よくあるつまずき

### Q. `open_examples.bat` が「CMake が見つかりません」と出る

**A.** CMake をインストール：
- 方法1: [https://cmake.org/download/](https://cmake.org/download/) から公式インストーラ
- 方法2: PowerShell で `winget install Kitware.CMake`

### Q. 「Visual Studio のバージョンが見つかりません」的エラー

**A.** [Visual Studio 2022 Community](https://visualstudio.microsoft.com/ja/downloads/)（無料）をインストール。
インストール時に **「C++ によるデスクトップ開発」** ワークロードにチェックを入れてください。

### Q. サンプルがビルドエラーになる

**A.** 以下を試してください：

1. **まず `rebuild_gxlib.bat` でクリーンビルド**（cache由来のエラー解消）
2. それでもダメな場合: 基本7本（01-05, 07, 09）は動作確認済み。上級6本（06, 08, 10-13）は `GX_BUILD_ADVANCED_EXAMPLES=ON` で opt-in。API 名の手修正が必要な場合があります。
3. 特定サンプルのエラーが消えない場合は GitHub Issues で報告してください。

### Q. git pull したらビルドが通らない

**A.** `rebuild_gxlib.bat` でクリーンビルド。古いビルドキャッシュが原因のことが多いです。

### Q. 黒画面のまま何も表示されない

**A.** Visual Studio のデバッグ出力ウィンドウ（出力 → デバッグ）を見てください。GXLib は失敗時に必ずログ出力するようになっています（ファイルが見つからない、フォーマット非対応など）。

---

## 📚 もっと知りたい

- `examples/README.md` — サンプル一覧と詳細
- `docs/engine-reference/gxlib/accessibility-scorecard.md` — どの機能がどこまで整備されているか
- `docs/architecture/` — エンジン設計の意思決定ログ（ADR）
- `CHANGELOG.md` — リリース履歴

---

## 🛠 上級者向け

コマンドラインから直接ビルドしたい場合：

```bash
cmake -B build -S .
cmake --build build --config Debug
```

`GX_BUILD_EXAMPLES=OFF` で examples を除外可能。

---

困ったら GitHub Issues で気軽に質問してください 🙌
