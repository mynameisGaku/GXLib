# Walkthrough3D

3D シーン内を自由移動できるウォークスルーのサンプルです。

## 操作
- WASD / QE: 移動
- Shift: 高速移動
- 右クリック: マウスキャプチャ切り替え
- ESC: 終了

## ビルド/実行
- ルートで `cmake -B build -S .`
- `cmake --build build --config Debug --target Walkthrough3D`
- `build/` 配下に生成される `Walkthrough3D.exe` を実行
