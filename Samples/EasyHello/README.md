# EasyHello

GXFramework の最小構成サンプル。シーン/描画/入力の流れを確認できます。

## 操作
- 矢印キー: 円を移動
- ESC: 終了

## ビルド/実行
- ルートで `cmake -B build -S .`
- `cmake --build build --config Debug --target EasyHello`
- `build/` 配下に生成される `EasyHello.exe` を実行
